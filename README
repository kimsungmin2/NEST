아직 하지 못한 것
1. 싱글톤 패턴(3월 11일에 배워서 적용 못 함) 현재 리포지토리를 직접적으로 생성해서 사용하고 있다면 이를 각 서비스에서 만들어서 모듈을 통해 익스포트 해서 사용 (이러면 단일 책임 원칙에서 자유로움 단, 서비스 간 순환 의존성 문제 생김 이건 고민 해봐야될듯)//끝남 
2. 커스텀 데코레이터 생성(아직 직접 생성해본게 없어서 한번 만들어야 할거 같음)
3. 커스텀 인터셉터(이 부분은 직접 티켓 확인하는 방식으로 만들어봤는데 좀 더 다양한 방식으로 접근 해봐야 될 듯)
4. 커스텀 파이프(아직 직접 구현 못해봄)
5. 쿼리빌더(락킹, sum, 검색 등 사용해봤지만 많은 쿼리 빌더를 이용해봐야 됨)
6. 전략 패턴 공부(문쌤 강의 덕분에 얼추 파악하고 있지만 제일 큰 문제는 조금밖에 써보질 못했다 정도(이 마저도 가드에서만 써봄 직접적으로 어디서 써야 되는지 모름))
7. 테스트 코드(진짜 위 코드 전부 구현하면 안되겠지만, 된다면 그 때 작성)